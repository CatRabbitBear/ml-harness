# Local development defaults (do not commit .env with real values)
CORE_ARTIFACT_ROOT=.artifacts
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT=ml-harness-dev
FX_RV_MLFLOW_EXPERIMENT=fx-rv-regression

# Docker compose defaults (example)
# CORE_ARTIFACT_ROOT=/shared/artifacts
# MLFLOW_TRACKING_URI=http://mlflow:5000

# Dataset artifact root used by local run scripts
DATASET_PATH=
